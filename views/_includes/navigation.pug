nav.navigation
  a.navigation__link(href='/' data-template='home')=meta.data.title
    .navigation__list__link#home__navigation__link Home
    
  ul.navigation__list
    each item in navigation.data.list
      li.navigation__list__item
        a.navigation__list__link(href=Link(item.link))=item.text
  

